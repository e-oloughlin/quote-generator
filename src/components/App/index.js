import { Container } from '@material-ui/core';
import Logo from '../Logo';
import './App.css';

const App = () => (
  <>
    <Container maxWidth="sm">
      <Logo />
    </Container>
  </>
);

export default App;
